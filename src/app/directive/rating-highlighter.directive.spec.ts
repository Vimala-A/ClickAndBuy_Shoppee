import { RatingHighlighterDirective } from './rating-highlighter.directive';

describe('RatingHighlighterDirective', () => {
  it('should create an instance', () => {
    const directive = new RatingHighlighterDirective();
    expect(directive).toBeTruthy();
  });
});
