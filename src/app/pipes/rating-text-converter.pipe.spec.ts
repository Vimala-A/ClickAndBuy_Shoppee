import { RatingTextConverterPipe } from './rating-text-converter.pipe';

describe('RatingTextConverterPipe', () => {
  it('create an instance', () => {
    const pipe = new RatingTextConverterPipe();
    expect(pipe).toBeTruthy();
  });
});
